@inherits BaseComponent

<p class=@_builder>
    @ChildContent
</p>

@code {
    [Parameter]
    public RenderFragment? ChildContent { get; set; }

    protected override void OnInitialized()
    {
        _ = _builder.Append("common-paragraph");
        base.OnInitialized();
    }
}
