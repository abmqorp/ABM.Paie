@using Common.Components.Bases
@inherits Base

<p class=@_builder.Add("common__paragraph")>
    @ChildContent
</p>

@code {
    [Parameter, EditorRequired]
    public RenderFragment ChildContent { get; set; }
}
