@using Common.Models
@inherits BaseComponent

@{
    _ = _builder.Add("common__quote"); 
}

<div class=@_builder>
    <span>@Model?.Text</span> <span class="name">@Model?.Author</span>
</div>

@code {
    [Parameter]
    public QuoteModel? Model { get; set; }
}
