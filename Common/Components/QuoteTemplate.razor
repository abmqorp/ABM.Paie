@using Common.Models
@inherits BaseComponent

<div class=@_builder>
    <span>@Data?.Text</span> <span>@Data?.Author</span>
</div>

@code {
    [Parameter]
    public QuoteData? Data { get; set; }

    protected override void OnInitialized()
    {
        _ = _builder.Append("common-quote-template");
        base.OnInitialized();
    }
}
